@model DepartmentModule.Models.Subject

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Subject</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" >
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Program" class="control-label"></label>
                <select asp-for="Program.Id" class="form-control" asp-items="ViewBag.Program.Id">
                    <option value="">-- Select Department --</option>
                </select>
                <span asp-validation-for="Program.Id" class="text-danger" />
            </div>
            <div class="form-group">
                <label asp-for="Themes" class="control-label"></label>
                <select asp-for="Themes.Id" class="form-control" asp-items="ViewBag.Themes.Id">
                    <option value="">-- Select Department --</option>
                </select>
                <span asp-validation-for="Themes.Id" class="text-danger" />
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
